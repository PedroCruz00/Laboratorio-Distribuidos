services:
  backend1:
    build: ./Back-end
    ports:
      - "3004:3004"
    environment:
      - PORT=3004

  backend2:
    build: ./Back-end
    ports:
      - "3005:3005"
    environment:
      - PORT=3005

  middleware:
    build: ./Middleware
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - BACKEND_URL_1=http://backend1:3004
      - BACKEND_URL_2=http://backend2:3005

  frontend:
    build: ./Front-end
    ports:
      - "9000:9000"
    environment:
      - PORT=9000
